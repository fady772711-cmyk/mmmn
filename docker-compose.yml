
version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
    # Uncomment if you want to pass API key via env vars
    # - API_KEY=your_gemini_api_key_here
